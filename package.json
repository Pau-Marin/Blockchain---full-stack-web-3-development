{
  "dependencies": {
    "ethers": "^5.6.9",
    "fs": "^0.0.1-security",
    "solc": "0.8.7-fixed"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node_modules/ --base_path . -o . SimpleStorage.sol"
  }
}
